# Frontend - Satya Certificate System

## Run
```bash
cd frontend
npm install
npm run dev
```

Frontend URL: http://localhost:5173
Backend API URL default: http://localhost:8080/api

## Flow
Student register/login -> enroll course -> request certificate -> admin approves -> student gets certificate link -> preview/download PDF/PNG -> public verify by serial number.
