import { Link } from 'react-router-dom'
import { getUser, logout } from '../utils/api'
export default function Nav(){ const user=getUser(); return <nav className="sticky top-0 z-50 bg-[#080916]/90 backdrop-blur border-b border-white/10"><div className="max-w-6xl mx-auto px-4 h-16 flex items-center justify-between"><Link to="/" className="font-bold text-gold-500">STA Certificate System</Link><div className="flex gap-3 items-center text-sm"><Link to="/verify" className="text-slate-300">Verify</Link>{user?.email?<><Link to={user.role==='ADMIN'?'/admin':'/student'} className="text-slate-300">Dashboard</Link><button onClick={logout} className="px-3 py-2 bg-white/10 rounded-lg">Logout</button></>:<><Link to="/login">Login</Link><Link to="/register" className="px-3 py-2 bg-gold-500 text-black rounded-lg font-bold">Register</Link></>}</div></div></nav> }
